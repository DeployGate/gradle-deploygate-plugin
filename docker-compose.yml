services:
  app:
    build:
      context: ./docker/mock-server
    tty: true
    volumes:
      - app-pid-data:/usr/src/app/tmp/pids
      - ./docker/mock-server:/usr/src/app
    ports:
      - '3000:3000'

volumes:
  app-pid-data:
    driver: local